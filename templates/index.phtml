<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Jekyll v3.8.5">
    <title>Login</title>

    <!-- Bootstrap core CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }

      html, body {
        height: 100%;
      }

      body {
        display: -ms-flexbox;
        display: flex;
        -ms-flex-align: center;
        align-items: center;
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: #f5f5f5;
      }

      .form-signin {
        width: 100%;
        max-width: 330px;
        padding: 15px;
        margin: auto;
      }
      .form-signin .checkbox {
        font-weight: 400;
      }
      .form-signin .form-control {
        position: relative;
        box-sizing: border-box;
        height: auto;
        padding: 10px;
        font-size: 16px;
      }
      .form-signin .form-control:focus {
        z-index: 2;
      }
      .form-signin input[type="email"] {
        margin-bottom: -1px;
        border-bottom-right-radius: 0;
        border-bottom-left-radius: 0;
      }
      .form-signin input[type="password"] {
        margin-bottom: 10px;
        border-top-left-radius: 0;
        border-top-right-radius: 0;
      }

      #alert-container {
        height: 50px;
      }

      #alert-msg {
        display: none;
      }

    </style>


  </head>
  <body class="text-center">
    <form class="form-signin" onsubmit="checkLogin(); return false;">
      <h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
      <label for="inputEmail" class="sr-only">Email address</label>
      <input type="email" id="inputEmail" class="form-control" placeholder="Email address" required autofocus>
      <label for="inputPassword" class="sr-only">Password</label>
      <input type="password" id="inputPassword" class="form-control" placeholder="Password" required>
      <div class="checkbox mb-3">
        <label>
          <input type="checkbox" value="remember-me"> Remember me
        </label>
      </div>
      <button class="btn btn-lg btn-primary btn-block">Sign in</button>
      <div id="alert-container" class="mt-2">
        <div id="alert-msg" class="alert alert-danger" type="submit" role="alert"></div>
      </div>
      <p class="mt-5 mb-3 text-muted">&copy; 2019</p>
      <button id="btn-check" type="button" class="btn btn-primary" onclick="checkUrl();">Check url</button>
      <a href="http://localhost/proyectos/api-slim/public/test/test"><button id="btn-link" type="button" class="btn btn-primary">Link</button></a>
    </form>

    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>

    <!-- Bootstrap -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>

    <script>

      function checkLogin(){

          $("#alert-msg").hide();

          var mail = $("#inputEmail").val();
          var pass = $("#inputPassword").val();

          var data = JSON.stringify({
            email: mail,
            password: pass
          });
 
          $.ajax({
            type: "POST",
            url: "http://localhost/proyectos/api-slim/public/auth/login",
            contentType: "application/json",
            dataType: "json",
            data: data,
            success: function(data, textStatus, jqXHR){
              if(data.token){
                localStorage.setItem("token", data.token);
                localStorage.setItem("timestamp", new Date().getTime());
              }else{
                $("#alert-msg").text(data.message);
                $("#alert-msg").fadeIn();
                localStorage.clear();
              }
              console.log(localStorage);
            },
            error: function(jqXHR, textStatus, errorThrown){
              alert('Error: ' + textStatus);
            }
          });

        }

        function checkUrl(){

          console.log(localStorage);

          if(!localStorage.token){
            console.log("no hay token");
            return false;
          }

          now = new Date().getTime();
          var expired = now - parseInt(localStorage.timestamp);
          if(expired>2000){
            console.log("token expirado");
            return false;
          }

          $.ajax({
            type: "GET",
            beforeSend: function(xhr){
                xhr.setRequestHeader('Authorization', 'Bearer ' + localStorage.token);
            },
            url: "http://localhost/proyectos/api-slim/public/api/user",
            contentType: "application/json",
            success: function(data, textStatus, jqXHR){
              // console.log(data);
            },
            error: function(jqXHR, textStatus, errorThrown){
              alert('Error: ' + textStatus);
            }
          });

        }

    </script>

  </body>
</html>